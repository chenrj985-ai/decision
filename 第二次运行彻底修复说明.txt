第二次运行彻底修复版
====================

本次不再对 recommendation_history.csv 读取后的 DataFrame 原地写入浮点数。

改为：
1. CSV读取后转为普通Python字典；
2. 在字典中更新最新价格、最大收益、最小收益和天数；
3. 重新创建全新的DataFrame；
4. 显式设置数值列与文本列类型；
5. 再保存CSV。

该方式绕开 pandas 3.x 严格 StringDtype 的限制，
可连续运行多次。

同时增加：
logs\last_error.txt 完整堆栈信息。
以后如果还有错误，会明确显示具体文件和行号，不再只有一句错误信息。
